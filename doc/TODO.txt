TODO:

- Replace incoming_messages table/model with text_messages(:direction => [:inbound vs. :outbound])

- In process_message, create list of text chunks to add to outgoing message

- Save outgoing messages

- User.has_many :voters

- Add state to handle when the voter search fails and they say they haven't voted in Wisconsin before

- Add state to handle when the voter search fails and they say they HAVE voted in Wisconsin before ('A common mistake is first name abbreviations, Greg vs. Gregory, etc')

- Add web backend

- Log searches

- getexceptional

- Figure out why post-deploy failure is happening

  http://devcenter.heroku.com/articles/using-compass